



// write a program that declares several int and float variables, without initialzing them, and then print their values, is there any pattern to the values?

//ANSWER: there is no pattern to these values, they are completely random. 

#include <stdio.h>

int main(void) {


	int i, j, k, l;
	float a, b, c, d;

	printf("value of i, j, k. l: %d, %d, %d, %d", i, j, k, l); 
	printf("value of a, b, c, d: %f, %f, %f, %f", a, b, c, d); 


	return 0; 

}
